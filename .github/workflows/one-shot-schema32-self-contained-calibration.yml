name: One-shot schema32 self-contained calibration
on:
  push:
    branches: [main]
    paths: ['.github/workflows/one-shot-schema32-self-contained-calibration.yml']
permissions:
  contents: write
jobs:
  calibrate:
    if: github.event.head_commit.message == 'run schema32 self-contained calibration'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install requests
      - name: Run self-contained PE/PB/peer calibration
        run: python scripts/one_shot_schema32_valuation_calibration_v3.py
      - name: Cleanup
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm scripts/one_shot_schema32_valuation_calibration_v3.py .github/workflows/one-shot-schema32-self-contained-calibration.yml
          git commit -m "ci: remove schema32 self-contained calibration"
          git push
